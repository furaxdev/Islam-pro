# Expo / React Native Metro bundler does not follow pnpm's symlinked
# node_modules well, so use a flat (hoisted) layout like npm/yarn.
node-linker=hoisted
# Let Expo/RN native tooling reach their transitive deps.
shamefully-hoist=true
