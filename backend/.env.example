# Backend environment variables — copy this file to backend/.env and fill in your values.
# The frontend needs NO env vars (it calls public APIs directly).

# MongoDB connection string (e.g. from MongoDB Atlas)
MONGO_URL=mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority

# Database name
DB_NAME=islampro
