# API environment variables — copy to apps/api/.env and fill in.
# Mongo is optional: /api/health works without it; /api/status needs it.

MONGO_URL=mongodb+srv://user:password@cluster.mongodb.net/?appName=IslamPro
DB_NAME=islampro
PORT=8000
